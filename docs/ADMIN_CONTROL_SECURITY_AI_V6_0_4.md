# MavenLex v6.0.4 — Admin Control Center + Security + AI Readiness

- Owner-only admin control center for text, button, frame and role-panel settings.
- Owner-only role assignment endpoint and UI guard.
- Public config endpoint so home text can be edited from Admin without code changes.
- AI status endpoint with local fallback message when Yandex keys are not configured.
- Account/security/readability polish for dark and light themes.
- Render-friendly package with prebuilt dist.
