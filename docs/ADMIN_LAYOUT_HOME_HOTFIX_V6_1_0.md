# MavenLex v6.1.0 — Admin Button + Home + Account Layout Hotfix

## Fixed

1. Owner Design Studio buttons now apply changes immediately in the interface.
2. Preset buttons now show a clear message and update the visible brand colors without waiting for refresh.
3. Save button text confirms that settings were saved and applied.
4. Home page was shortened: the long explanatory blocks are hidden from the main screen.
5. Home hero lead is capped to avoid long text blocks.
6. Account security layout was rebuilt so email, role, sessions and last login no longer overlap.
7. Long email, user-agent, status and date text now wraps inside cards.
8. Admin color inputs, role panel inputs and design buttons have stronger z-index/pointer handling.

## Deployment

Render settings stay the same:

Build Command: npm install
Start Command: node server.js
Root Directory: empty
